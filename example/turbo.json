{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*", "**/wrangler.toml"],
  "pipeline": {
    "fmt": {},
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "outputs": ["dist", "build", "lib"]
    }
  }
}
